[project]
name = "github-stats"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "aiohttp>=3.14.3",
    "httpx>=0.28.1",
]

[dependency-groups]
dev = [
    "ruff>=0.16.8",
    "ty>=0.0.83",
]
